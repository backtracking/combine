begin program "sudoku"
  files = [ "sudoku.ml" ]
  requires = [ "interp" "str" ]
end

begin program "queens"
  files = [ "queens.ml" ]
  requires = [ "interp" ]
end

begin program "backslide"
  files = [ "backslide.ml" ]
  requires = [ "interp" ]
end
